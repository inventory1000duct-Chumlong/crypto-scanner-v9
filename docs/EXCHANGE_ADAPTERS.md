# V29 Exchange Adapters

Endpoints:
- GET /api/exchanges
- GET /api/exchanges/health
- GET /api/markets/aggregate
- GET /api/markets/spread/:symbol

V29 creates the adapter framework. Some exchange adapters are adapter-ready, not full live integrations yet.
